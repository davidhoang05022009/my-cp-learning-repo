// This is a file contains my most used macros
// I typed this file by hand so that I can remember
// the template to use in contests

#include<bits/stdc++.h>
using namespace std;
using ll = long long;
using db = double;
#define pb push_back
#define endl "\n"
const int MOD = 1e9 + 7;

// Remember to always declare arrays/vectors globally
// to avoid quirks

// GCD and LCM functions for some problems
// ll gcd(ll a, ll b) {
//     if (b == 0) return a;
//     return gcd(b, a % b);
// }
// ll lcm(ll a, ll b) {
//     return (a / gcd(a, b)) * b;
// }

int main() {
    ios::sync_with_stdio(0);
    cin.tie(nullptr);
    // freopen("template.in", "r", stdin);
    // freopen("template.out", "w", stdout);
    return 0;
}

// Declare functions before main() bro!